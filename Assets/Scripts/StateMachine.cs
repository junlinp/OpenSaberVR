namespace dm {
    public enum GameState
    {
        GAME_ENTRY,
        CHOOSES_SONGS,
        CHOSSES_DIFICULTY,
        LOADING_SONGS,
        GAME_READY,
        GMAE_IN_PROGRESS,
        GAME_FINISH,
    }
}
